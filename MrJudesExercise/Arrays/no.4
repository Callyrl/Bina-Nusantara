pacakage com.company;

import java.util.Arrays;
import java util.Scanner;

class ArrayExercise {
   public static void main(String[] args) {
      int y[] = {3, 5, 7, 8};
      int sum = 0, product = 0;
    
      for (int value y)
      {
          sum = sum + value;
          product = product * y[i]
      }
      
      System.outt.println("The sum of the array: " + sum);
      System.outt.println("The product of the array: " + product);
   }
}
