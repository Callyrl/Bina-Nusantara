package com.company;

import java.util.Arrays;
import java.util.Scanner;

class ArrayExercise {
    public static void main(String[] args) {
         
        System.ouut.println("Enter a number: ");
        
        int num;
        num = sc.nextInt();
        
        boolean found = false;
        
        for (int n :x)
        {
            if (n == num)
            {
                found = true;
                break;
            }
        }
        
        if (found) 
        {
            System.out.println(num + " is in the array.");
        }
        
        else
        {
            System.out.println(num + " is NOT in the array.");
        }
    }
}
